[build]
  command = "npm run build"
  publish = ".next"
  node_version = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Let the Next plugin output its own functions here
  directory = ".netlify/functions-internal"
  node_bundler = "esbuild"
